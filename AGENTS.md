# envforge-agent

The guidance for this repository is in [CLAUDE.md](CLAUDE.md). Read that.

This file used to be a copy of it. Two copies of one set of instructions is the same
mistake this project keeps writing rules about: on 2026-09-03 a review found that the
copy still described a hand-written provider layer and two engines behind one interface,
both deleted, because an edit had updated one file and not the other. A pointer cannot
drift.
